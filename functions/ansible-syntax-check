# ansible-syntax-check
ansible-syntax-check() {
  local _funcret=0
  ansible-test-run --syntax-check; _funcret=$?
  [ $_funcret -eq 0 ] && {
    echo "OK."
  }
  return $_funcret
}
# vim: set ff=unix ts=2 sw=2 sts=2 et : This line is VIM modeline
