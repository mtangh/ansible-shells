---
# test file for {{ role_name }}
# vim: set ff=unix ts=2 sw=2 sts=2 et : This line is VIM modeline

- hosts: localhost
  connection: local
  gather_facts: no

  tags:
    default

  vars:

  roles:
  - test-setup
  - { role: ../.. }
  - test-teardown

